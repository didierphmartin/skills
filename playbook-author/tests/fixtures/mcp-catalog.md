# Available MCP Functions

## Okta

### Functions
- `mcp_lookup_users` — Look up users by email.
- `mcp_reset_password` — Reset a user's password.

## Workday

### Functions
- `mcp_get_pto_balance` — Get a worker's time-off balances.
- `mcp_submit_time_off` — Submit an approved time-off request.
